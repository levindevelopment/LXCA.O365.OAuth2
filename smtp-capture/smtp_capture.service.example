[Unit]
Description=SMTP Capture (LXCA OAuth2 validation)
After=network-online.target

[Service]
Type=simple
ExecStart=/usr/bin/python3 /opt/smtp-capture/smtp_capture.py
WorkingDirectory=/opt/smtp-capture
Restart=on-failure
User=root

[Install]
WantedBy=multi-user.target
